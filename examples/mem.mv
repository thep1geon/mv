str "Hello World!" ;; pushes the first pointer to the stack and writes the string to a chunk of memory
pop 0 ;; move the string pointer to register 0

mov 0
strlen .
pop 1
pop
mov 1

pop 1 ;; move the strlen into register 1

mov 1

;; reg 0: 0
;; reg 1: 12
;; reg 9: 12
;;  

print_str:
pop 9
mov 0

mem_read .

pop 8
pop 7 ;; swapping the top two elements on the stack
mov 8
mov 7

inc 
pop 0
mov 9
dec 0
jmp_neq print_str 0
pop

print
